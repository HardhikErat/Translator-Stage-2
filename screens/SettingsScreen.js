import{ React, component } from 'react';
import { View, Text, TouchableOpacity } from 'react-native-elements';

export default class SettingsScreen extends Component{
    render() {
        return(
            <View>
                <Text>Settings Screen</Text>
            </View>
        )
    }
}